package ua.step.homework;

/**
 * Задание 6
 * 
 * Есть прямоугольник у которого известна ширина width и высота height, найти и
 * вывести на консоль периметр и площадь заданного прямоугольника. Высота и
 * ширина прямоугольника должна задаваться переменными в коде программы. Вывод:
 * сначала периметр, через пробел площадь
 *
 */
public class Task06 {
    public static void main(String[] args) {
        int width = 10;
        int height = 3;

        // две строки кода ниже нужны для тестирования (@see TestTask06.java)
        width = (args.length == 2) ? Integer.parseInt(args[0]) : width;
        height = (args.length == 2) ? Integer.parseInt(args[1]) : height;

        // используй переменные width и height для нахождения результата

        // FIXME: Здесь будет ваш код. Удалите эту строку после выполнения задания
    }
}